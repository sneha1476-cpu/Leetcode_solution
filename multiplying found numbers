int findFinalValue(int* nums, int numsSize, int original) {
    int i;
    for (i = 0; i < numsSize; i++) {
        if (nums[i] == original) {
            original = original * 2;
            i = -1; 
        }
    }
    return original;
}
